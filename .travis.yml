language: java
notifications:
  email:
  - francisco.solans@neodoo.es
jdk:
  - openjdk6
before_install:
  - cp .travis.maven.settings.xml $HOME/.m2/settings.xml

script:
  - travis_wait ./mvn clean install -q -Dmaven.test.skip=true -Dinit
  - travis_wait ./mvn clean install -q -Dmaven.test.skip=true -Djars
  - travis_wait ./mvn clean install -q -Dmaven.test.skip=true
  - travis_wait ./mvn clean install -q -Dmaven.test.skip=true -Dwars
  - travis_wait ./mvn clean install -q -Dmaven.test.skip=true -Dears -P generate-distri

